diff --git a/node_modules/tiny-slider/dist/tiny-slider.js b/node_modules/tiny-slider/dist/tiny-slider.js
index 1b5d912..30d5771 100644
--- a/node_modules/tiny-slider/dist/tiny-slider.js
+++ b/node_modules/tiny-slider/dist/tiny-slider.js
@@ -563,7 +563,8 @@ var tns = function (options) {
     freezable: true,
     onInit: false,
     useLocalStorage: true,
-    nonce: false
+    nonce: false,
+    textDirection: 'ltr'
   }, options || {});
   var doc = document,
       win = window,
@@ -2898,6 +2899,15 @@ var tns = function (options) {
       val = getContainerTransformValue();
     }

+    // RTL: flip transform direction only for horizontal sliders
+    if (horizontal && options.textDirection === 'rtl') {
+      if (val.charAt(0) === '-') {
+        val = val.substr(1);
+      } else if (val.charAt(0) !== '-') {
+        val = '-' + val;
+      }
+    }
+
     container.style[transformAttr] = transformPrefix + val + transformPostfix;
   }

@@ -3310,7 +3320,10 @@ var tns = function (options) {
     var keyIndex = [KEYS.LEFT, KEYS.RIGHT].indexOf(e.keyCode);

     if (keyIndex >= 0) {
-      onControlsClick(e, keyIndex === 0 ? -1 : 1);
+      // RTL: reverse arrow key direction only for horizontal sliders
+      var dir = keyIndex === 0 ? -1 : 1;
+      if (horizontal && options.textDirection === 'rtl') { dir = -dir; }
+      onControlsClick(e, dir);
     }
   } // on key control

@@ -3320,7 +3333,9 @@ var tns = function (options) {
     var keyIndex = [KEYS.LEFT, KEYS.RIGHT].indexOf(e.keyCode);

     if (keyIndex >= 0) {
-      if (keyIndex === 0) {
+      // RTL: reverse arrow key direction only for horizontal sliders
+      var isLeft = (horizontal && options.textDirection === 'rtl') ? keyIndex === 1 : keyIndex === 0;
+      if (isLeft) {
         if (!prevButton.disabled) {
           onControlsClick(e, -1);
         }
@@ -3481,6 +3496,9 @@ var tns = function (options) {
       var x = translateInit,
           dist = getDist(lastPosition, initPosition);

+      // RTL: reverse drag direction only for horizontal sliders
+      if (horizontal && options.textDirection === 'rtl') { dist = -dist; }
+
       if (!horizontal || fixedWidth || autoWidth) {
         x += dist;
         x += 'px';
@@ -3511,6 +3529,9 @@ var tns = function (options) {
       lastPosition.y = $.clientY;
       var dist = getDist(lastPosition, initPosition);

+      // RTL: reverse drag direction only for horizontal sliders
+      if (horizontal && options.textDirection === 'rtl') { dist = -dist; }
+
       if (Math.abs(dist)) {
         // drag vs click
         if (!isTouchEvent(e)) {
@@ -3528,12 +3549,14 @@ var tns = function (options) {

         if (carousel) {
           rafIndex = raf(function () {
+            // RTL: use calcDist for index calculation only for horizontal sliders
+            var calcDist = dist;
             if (horizontal && !autoWidth) {
-              var indexMoved = -dist * items / (viewport + gutter);
-              indexMoved = dist > 0 ? Math.floor(indexMoved) : Math.ceil(indexMoved);
+              var indexMoved = -calcDist * items / (viewport + gutter);
+              indexMoved = calcDist > 0 ? Math.floor(indexMoved) : Math.ceil(indexMoved);
               index += indexMoved;
             } else {
-              var moved = -(translateInit + dist);
+              var moved = -(translateInit + calcDist);

               if (moved <= 0) {
                 index = indexMin;
@@ -3545,7 +3568,7 @@ var tns = function (options) {
                 while (i < slideCountNew && moved >= slidePositions[i]) {
                   index = i;

-                  if (moved > slidePositions[i] && dist < 0) {
+                  if (moved > slidePositions[i] && calcDist < 0) {
                     index += 1;
                   }

@@ -3558,8 +3581,10 @@ var tns = function (options) {
             events.emit(isTouchEvent(e) ? 'touchEnd' : 'dragEnd', info(e));
           });
         } else {
+          // Gallery mode: RTL reverses swipe direction only for horizontal sliders
           if (moveDirectionExpected) {
-            onControlsClick(e, dist > 0 ? -1 : 1);
+            var dir = dist > 0 ? -1 : 1;
+            onControlsClick(e, dir);
           }
         }
       }
